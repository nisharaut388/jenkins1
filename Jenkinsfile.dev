pipeline {
    agent any

    stages {
        stage('Code fetch') {
            steps {
                echo 'Code Fetch from github'
                git url :'https://github.com/nisharaut388/jenkins1.git', branch: 'dev'
                
            }
        }
        
        stage('Build') {
            steps {
                sh 'docker build -t my-new-website_dev .'

            }
        }
        
        stage('Deploy') {
            steps {
                echo 'Deploy on Container'
                //sh 'docker stop $(docker ps -q) && docker system prune -a '
                sh 'docker run -d -p 80:80 my-new-website_dev '
            }
        }
    }
}
